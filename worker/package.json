{
  "name": "aurelia-multipliers-worker",
  "version": "0.1.0",
  "description": "AURELIA-MULTIPLIERS Cloudflare Worker — Telegram webhook + GitHub Actions control plane. Carried over from upstream aurelia in Part 1 (no behavior changes yet); Multiplier-specific commands land in Part 2.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
